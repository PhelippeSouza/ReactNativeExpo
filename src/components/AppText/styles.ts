import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textRegular: {
  },

  textBlack: {
    color: '#000000',
  },

  textWhite: {
    color: '#FFFFFF',
  },
});

export default styles;
